spring:
  application:
    name: payment-service

  profiles:
    active: local

server:
  port: 8087

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    root: INFO


