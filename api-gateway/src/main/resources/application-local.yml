#server:
#  port: 8080
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#spring:
#  cloud:
#    gateway:
#      discovery:
#        locator:
#          enabled: true
#      routes:
#        - id: auth-service
#          uri: lb://auth-service
#          predicates:
#            - Path=/auth/**
#          filters:
#            - RewritePath=/auth/(?<path>.*), /$\{path}
#        - id: product-service
#          uri: lb://product-service
#          predicates:
#            - Path=/product/**
#          filters:
#            - RewritePath=/product/(?<path>.*), /$\{path}

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
      routes:
        - id: auth-service
          uri: http://localhost:8085
          predicates:
            - Path=/auth/**
        - id: catalogue-service
          uri: http://localhost:8084
          predicates:
            - Path=/api/categories/**, /api/products/**
        #filters:  # Uncomment if needed later
        #  - RewritePath=/product/(?<path>.*), /${path}

eureka:
  client:
    enabled: true



